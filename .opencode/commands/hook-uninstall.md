---
description: 从指定客户端（或全部）卸载一个 hook。
agent: hook-manager
---

# /hook-uninstall <name> [--client opencode|cursor|kimi|all]

按锚点注释 `# managed-by: agent-hook · <name>` 从客户端配置中删除对应段落。**不删** `registry/<name>/`。
